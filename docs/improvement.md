In location we should show user the dropdown from which we can select the location
In budget numerical value we can input rather then low medium high
(change the logic in the prompt as well)
shortlist size is not required 
we can fix amount of restaurants we are sending to the model